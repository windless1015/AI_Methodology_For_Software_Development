# AI 辅助复杂软件工程方法论

**Agentic Engineering Playbook v1.0 · 2026-08-27**

> 适用范围：单人或小团队 + AI coding agent，开发长周期、高复杂度、正确性敏感的软件
> （科学计算、数值仿真、基础设施、工业软件）。
> 来源：FHCoupler 项目数月实战教训 + 双 AI 交叉审阅 + 2025–2026 agentic coding 生态调研
> （GitHub Spec Kit / OpenSpec / Superpowers / PhysVEC / aislop / SlopCodeBench / compound engineering 等）。
> 本文档与具体项目无关，可直接作为新项目的启动标准。

---

## 第 0 章 总纲：四条第一性原理

本方法论的全部条款都从这四条推出。忘记所有细节时，记住这四句。

### 0.1 机制 > 文字

**陈述性知识对行为的约束力约等于零。**

实证：一个 AI 在回答结尾写下"文档里写一万条'不许编'都拦不住，一句 `curl -sI` 三秒就拆穿。机制 > 文字"——
而在写下这句话时，它已经对两个可访问的 URL 下了"存疑"判决，且没有 fetch 过任何一个。
它不仅读过那条规则，它刚刚亲手写下了那条规则，然后立刻违反了它。

推论：**一条规则只有在变成"不满足就跑不动"的时候才有效力。**
每一条规则都必须回答一个问题："它的执行钩子是什么？"（hook / CI gate / lint / schema / 测试）。
答不出来的规则不写——写了也是负资产，因为它制造了"已经有约束"的假象。

### 0.2 验证器先行

AI 生成代码的速度 ≫ 人判定对错的速度。瓶颈在验证，不在生成。

所以脚手架的核心不是"怎么让 AI 写得更好"，而是：

- **让验证比生成便宜**（短门禁先于长计算，确定性检查先于 LLM 审阅）；
- **让错误比正确更难通过**（物理不变量、schema、三值判决挡在每一层）；
- **验证器的期望值必须来自解析解、文献或已验证代码，绝不能来自代码自身的输出**——
  用自身输出标定的测试只能发现回归，无法发现系统性错误。

### 0.3 防假阴性，甚于防假阳性

AI 会把真实存在的东西判成假的（假阴性），这比凭空编造（假阳性）危险得多：

- 假阳性是**加入噪声**——早晚会在编译或跑数时撞上，有自然纠错通道；
- 假阴性是**删除正确信息**——没有任何自然纠错通道。如果你信了，正确选项永久消失，而你永远不会知道它曾在。

假阴性的两个产生机制：

- **机制 A：用弱仪器测，把 null 读成"不存在"。** 搜索引擎对小众仓库返回空、某个工况下测不出缺陷——
  仪器在"存在"和"不存在"两种情况下都返回空，判别力为零。用判别力为零的测量读出二值结论，是最贵的错误。
- **机制 B：带着"找问题"的任务去审，于是找到了"问题"。** 先验被设成"这里面应该有错"，
  不足的证据就会被结论接受。永远不要给 AI 下达"找出这里的问题"这种任务（见 §8.2 任务措辞规则）。

推论：**排除性结论的证据门槛必须高于确认性结论**（见 §3.3）。

### 0.4 目标不是不犯错的 agent

不存在不犯错的 agent。以上机制的目的不是让 AI 变可靠，而是：

> **把 AI 的错误变得便宜且必然被发现。**

让一个不可信的执行者无法造成持久损害：每个结论带分辨率声明、每个排除带阳性对照、
每条规则有执行钩子、每个反驳交证据。在这个体系里，错误在 30 秒内暴露，
而不是靠人恰好点开一个链接、恰好发现一个异常。

---

## 第 1 章 六大故障模式 → 机制对照总表

| # | 故障模式 | 典型症状 | 根因 | 对策机制（非文字） | 章节 |
|---|---|---|---|---|---|
| F1 | 方向幻觉 | AI 瞎想一个点就按这个方向做；调试时瞎猜乱改 | 无"改动提案"状态机；任务措辞诱导 | spec 状态机 + plan 批准门 + 任务措辞规则 + 三次失败停手 | §5, §8.2, §3.6 |
| F2 | 参数/配置错误 | 用错参数跑完整轮实验才发现 | 配置无契约、无 provenance | config schema 校验 + passport 指纹 + dry-run 回显 | §2.2 |
| F3 | 公式/符号错误 | 瞎编公式、符号错、参数不对；md 指令无效 | 公式无机器验证通道 | 三道闸：出处闸 + 机器闸（SymPy/量纲）+ 物理闸（解析极限/MMS） | §4 |
| F4 | 决策漂移 | ADR 被无视，或被频繁无痕迹推翻 | ADR 是被动文档；推翻零成本 | supersede 链 + 推翻必须交 artifact + 强制门补丁 | §5 |
| F5 | 代码腐化 | 一个函数几代代码并存；_v2/_old 满地；屎山 | agent 迭代单调累积 slop；兼容性策略让源码承载历史 | 确定性 slop 门 + 单一实现硬约束 + 测试承载历史 + 复杂性预算 | §6 |
| F6 | 验证假阴性 | 正确答案被错误排除，弯路几十天 | 弱仪器读二值结论；排除无门槛 | 三值判决 + 分辨率声明 + 阳性对照 + 证据强度表 | §3 |

F6 是最危险的：F1–F5 的错误都有机会在下游撞上现实，F6 没有。

---

## 第 2 章 第 0 天地基（顺序即优先级）

**这些必须先于任何功能代码存在。事后补的成本是十倍。**

### 2.1 不变量层（最高优先级）

**做什么**：在写求解器/核心逻辑之前，先写"物理/逻辑上必须成立"的不变量测试套件。

科学计算项目清单（按 PhysVEC 双验证器结构组织）：

- **解析解测试**：领域内所有有闭式解的退化情形（例：Stokes 沉降终速、Hertz 接触力-重叠曲线、
  Couette/Poiseuille 流、文献基准算例）。
- **守恒律残差**：质量/动量/能量审计，残差阈值进 CI 硬门（如 < 1e-3）。
- **极限退化**：转速=0 → 速度场恒零；粘度→∞ → 刚体跟随；颗粒数=0 → 退化单相。
- **对称性**：旋转/镜像对称的算例必须给出对称结果。
- **量纲断言**：带量纲的类型或单位换算层，量纲不匹配 = 编译错误/断言失败，不是结果错误。
- **收敛性**：网格/步长加密时结果必须收敛（MMS，Method of Manufactured Solutions）。

**为什么第 0 天**：这是唯一能跨重构、跨 AI 会话存活的断言层。代码会被 AI 反复改写，
不变量不会变。没有它，每次重构都是俄罗斯轮盘。

**验收**：空项目 CI 就亮绿灯；任何新功能合入前必须过全套不变量。

### 2.2 配置契约与指纹

**做什么**：

1. **Config schema 校验器**：加载时机械拒绝——未知键报错、类型检查、物理范围检查、
   跨字段一致性断言（例：dp < dx、时间步长上限、禁设字段互斥）。项目已知坑全部写成断言。
2. **Passport 指纹**：每次运行把 `config hash + 关键参数 + 代码版本(commit)` 写进输出目录的
   `passport.yaml`；所有分析/绘图脚本**先校验指纹再画图**。

**为什么第 0 天**：这是所有措施里最便宜的（半天），防的是"分析完才发现用的是旧配置"——
一整轮实验报废。F2 的唯一根治法。

**验收**：故意改错一个参数，运行被拒绝并指出具体字段；输出目录里一定有 passport.yaml。

### 2.3 判决门禁（hypothesis 预注册）

**做什么**：任何超过阈值时长（如 > 1 小时）的计算/实验，必须先存在一份已批准的
`hypothesis.yaml`（模板见附录 A），否则 hook 拦截不许启动。

**为什么**：artifact 要求也能被钻空子——AI 可以为稻草人断言生产证据。
预注册（先写假设、预期效应量、阳性对照，人批准，再跑）堵上这个口子。
这也是"一次只改一个变量"的载体。

### 2.4 CI 骨架与反压内环

**做什么**：

- CI 从第一个 commit 存在：build + 不变量测试 + lint + 确定性 slop 扫描（如 aislop），全绿才算完。
- Agent 工作流内置反压（backpressure）：改完代码**自动**跑检查、失败自己修，人只看意图层。
  规则："成功输出压缩成一行，失败输出完整可行动信息"——别把一千行日志倒进上下文。
- 检查分层：会话内跑快的（build/lint/单测）；PR 前跑全的（集成/不变量）；
  定期跑慢的（收敛性、长算例回归）。

### 2.5 治理文件三件套

1. **薄宪法 `AGENTS.md`（≤ 100 行）**：只写三档边界——
   `Always do`（不用问就做）/ `Ask first`（改物理模型、改控制方程、推翻 ADR、加依赖——必须先问）/
   `Never do`（禁设字段、禁止手动指定应由代码推导的量）。
   指令总量超过 ~150–200 条可靠性反而下降；细节全部指向分层文档。
2. **活架构图 `architecture.md`**：模块地图、数据流、热点、技术债。宪法说"怎么工作"，它说"在跟什么工作"。
3. **spec 状态机目录**（OpenSpec 式，见第 5 章）。

### 2.6 记忆环（lessons → 机器检查）

**做什么**：`lessons.md` 从第一天存在；每个教训按附录 E 格式记录。
**铁律：同一个坑踩第二次，当天必须产出一条机器检查（断言/lint/hook/schema 规则），不产出不算修完。**

文字教训管不住第二次，机器检查能。这是把"经验"变成"资产"的唯一通道。

---

## 第 3 章 判决与实验纪律（正确性敏感项目的核心）

### 3.1 三值判决 schema

**禁止任何判决输出二值 PASS/FAIL。** 所有实验/验证结论必须三值：

| 判决 | 含义 |
|---|---|
| **证实** | 效应被观测到，且超过噪声底 |
| **证伪** | 效应不存在，**且有阳性对照证明本测量能测到它** |
| **无分辨力** | 测量返回 null，但判别力不足以支撑"证伪" |

弱证据只能支撑"无分辨力"，永远不能支撑"证伪"。

### 3.2 预注册：hypothesis.yaml（先写后跑）

跑之前必须填写，填不出来就不许跑（模板见附录 A）。必填字段：

- 假设 H1 与预期效应量（定量，不允许"应该有变化"）
- 测量量与噪声底（噪声底来自重复实验，不是拍的）
- `可分辨: true/false`（预期效应量 ≫ 噪声底才为 true；false 时脚本只允许输出"无分辨力"）
- **阳性对照**：人为注入已知缺陷，确认诊断量按预期响应

### 3.3 排除性结论的高证据门槛

- "X 是根因"——需要**一次正面演示**。
- "X 不是根因"——需要**阳性对照**：先证明"如果 X 真的坏了，这个诊断量会变"，再拿"它没变"说事。

每次下排除结论前自问一句：**"如果 X 真的存在，我这个测量会找到它吗？"**
答不上来，结论只能是"无分辨力"。

### 3.4 工具 → null 结果证据强度表

每个项目维护自己的表，放进 AGENTS.md。示例（来自 FHCoupler 实战，通用化）：

| 观测 | null 的含义 | 强度 |
|---|---|---|
| grep 没找到 | 可能命名不同 / 被 ignore 排除 | 弱 |
| 编译器 undefined reference | 确实不存在 | 强 |
| 输出文件里看不到某场量 | 可能被输出掩码过滤 | 弱 |
| 守恒律残差 < 阈值 | 该量确实守恒 | 强 |
| 短算例没崩 | 长算例可能仍崩（延迟触发） | 弱 |
| 收敛指标达标 | 可能被数值扩散/抹平伪造 | 弱 |
| Web 搜索没找到 | 可能未被索引（小众/新仓库） | 弱 |
| 直接访问 URL 返回 404 | 确实不存在 | 强 |

规则：**弱证据行的 null 永远不许翻译成"不存在/无问题"。**

### 3.5 一次一变量 + 门禁阶梯

- **一次只改一个变量。** 多变量同时改的实验不产生任何可归因结论。
- **便宜的门先于贵的跑**：任何 > 1 小时的计算，必须先过 < 5 分钟的短门禁
  （纯流体稳定性 → 短冒烟 → 长生产）。门禁阶梯要制度化，不是靠自觉。

### 3.6 三次失败停手

调试纪律（源自 Superpowers systematic-debugging，与实战教训一致）：

1. 先根因调查：完整读错误、打边界日志、**必须能稳定重现**才许动手修。
2. 一次只改一处验证一个假说；假说失败就撤回，**不许在错误方向上继续加代码**。
3. **修了三次还没好 → 强制停下来质疑架构/前提本身**，而不是尝试第四次。

症状修复即失败。排除性假说按 §3.3 的高门槛执行。

---

## 第 4 章 公式与模型入库三道闸

任何公式/物理模型进入代码库，必须按顺序过三道闸。**md 文档里写"不许编公式"无效——
必须让公式过机器。**

| 闸 | 内容 | 落地 |
|---|---|---|
| **G1 出处闸** | 每个公式必须引用来源（文档方程号 / 文献编号），注释里写完整推导链 | code review 检查；无出处不合并 |
| **G2 机器闸** | 符号复核（SymPy 独立重算关键推导步）+ 量纲一致性断言 | 推导脚本入库，CI 重跑 |
| **G3 物理闸** | 解析极限退化 + 守恒律残差 + 对称性 +（适用时）MMS 收敛 | 进不变量套件（§2.1），CI 硬门 |

结构性要求（PhysVEC 模式）：**编程验证与科学验证分层**——
代码能跑（unit/integration test）≠ 物理对（物理断言）。
两层各自独立通过才算入库。

已知边界：SymPy 对高度抽象的张量运算覆盖有限，G2 过不了时不得静默跳过，
必须在注释和 ADR 里显式声明"此公式仅过 G1+G3"。

---

## 第 5 章 变更与决策治理

### 5.1 spec 状态机（OpenSpec 式目录）

```
openspec/
├── specs/            # 当前真相（唯一事实源）
├── changes/<name>/   # 在途提案：proposal.md + design.md + tasks.md + spec deltas
└── changes/archive/  # 已完成变更（含日期前缀，永久留痕）
```

流程：`explore（不写码的探讨）→ propose（提案，人批准）→ apply（按 tasks 实施）→ archive（delta 合并回 specs）`。

**关键补丁——强制门**：此类工具的默认门是建议性的（validator 只查结构不查行为，社区普遍抱怨 spec drift）。
必须自行补硬门：**无已批准 proposal 的功能性改动不许合并**（CI 检查 changes/ 目录状态），
否则状态机会退化成装饰。

### 5.2 决策记录与推翻纪律

- 决策用轻量 ADR（附录 D），带 `supersedes` / `superseded-by` 链。
- **推翻 ADR 是合法操作，但必须交 artifact**：反证的命令 + 输出 + 图，不接受纯论证。
  免费打出"存疑"两个字是被禁止的——因为要推翻的人一执行命令就会看到真相。
- ADR 频繁被推翻时，**修上游别修下游**：推翻频率高通常不是 ADR 制度问题，
  而是喂给 ADR 的判决质量不够。判决三值化（§3）之后，推翻频率会自然下降。
- 冲突处理：ADR 与计划冲突时，要么遵守 ADR，要么起草继任 ADR——**不许静默偏离**。

### 5.3 三档边界（写进 AGENTS.md）

- **Always do**：跑测试、遵循命名、更新 passport……
- **Ask first**：改物理/数学模型、改控制方程、推翻 ADR、新增依赖、改 CI/门禁本身。
- **Never do**：禁设字段（应由代码推导的量）、保留 _v2/_old/注释掉的旧实现、
  用代码自身输出标定测试期望值。

---

## 第 6 章 代码防腐

agent 迭代会**单调**累积 slop（SlopCodeBench 实测；行业数据：copy-paste 占比 2022→2026 从 9.4% 升到 15.7%，
重构占比从 21% 跌到 3.8%）。防腐靠机制，不靠提醒。

1. **单一实现硬约束**：禁止同一功能多版本并存（_v2/_old/注释保留）。lint + CI 强制，不写进 md 靠自觉。
2. **测试承载历史，不让源码承载历史**："保历史算例逐位可复现"是合理需求，
   正确做法是旧行为固化成回归测试的黄金输出，然后从源码删除旧路径。
   任何 legacy 兼容分支必须带 **sunset 日期**，到期迁移或删除。
3. **确定性 slop 门**：无 LLM 的确定性扫描（如 aislop：死代码、吞异常、重复 helper、
   幻觉 import、超大函数），post-edit hook 每次改完立刻回灌反馈；CI 设分数下限。
4. **独立清理 pass**：实现循环里的"重构"步骤会被 agent 缩水成改个变量名。
   每个里程碑跑一次独立清理（单独上下文，唯一任务：测试保持绿，消灭重复/死代码/不一致命名，不改行为）。
5. **复杂性预算**：函数行数 / 文件行数 / 嵌套深度阈值进 CI；超阈值触发人工评审。
   "一个函数里 30 个 if"不是风格问题，是下一代 bug 的孵化器。

---

## 第 7 章 记忆与升级环

AI 没有跨会话记忆，同样的错误会反复犯。解法不是更长的 CLAUDE.md，是升级管道：

```
错误发生 → 记入 lessons.md（附录 E 格式，廉价、频繁）
         → 同一教训第二次出现 → 当天升级为机器检查（断言/lint/hook/schema）
         → 定期 groom：对照当前代码重验旧教训，过期的删除（防止陈旧规则误导）
```

防污染规则：一次性偶然不升级（至少两次独立出现）；升级为规则前重验其仍然成立；
个人偏好不进共享规则库。

---

## 第 8 章 人机分工与多 AI 协作

### 8.1 人的角色

人的时间应该 **50% 花在定义"什么算对"**（不变量、spec、门禁、hypothesis 审批），
**50% 花在让系统变聪明**（教训升级、规则维护）——而不是 review 每一行代码。
AI 是高速生产者，人是架构师兼守门员。

### 8.2 任务措辞规则（防机制 B）

- ❌ 禁止："找出这段代码/这个结果的问题"——它会找到问题，不管问题是否存在。
- ✅ 正确："对下面每一条断言，给出证实或证伪的 artifact（命令+输出）。
  **允许并鼓励输出'全部成立'。**"
- 必须显式地、低成本地给出"没问题"这个出口，否则你得到的是为交差而生产的问题清单——
  这正是"AI 瞎想一个点然后按这个方向做"的上游成因。

### 8.3 多 AI 交叉审阅规则

不同模型的失败模式确实不重合，互审有价值。但：

- **裁决权归 artifact，不归修辞。** 没有 artifact 约束的交叉审阅，会让错误的一方看起来更权威——
  因为审阅者天然占据"我在检查"的位置。
- 审阅方每一条否定意见必须附可复现的命令和输出；附不出来的，不进入结论。
- 审阅任务同样遵守 §8.2：是"逐条证实/证伪"，不是"找茬"。

### 8.4 长任务工程

- 大任务拆成带确切文件路径和验证命令的小任务；每个任务用新鲜子代理执行（防上下文漂移）。
- 计划生成后设人批准门，再进入实施。
- 并行多 agent 时，ADR/spec 是防止各自漂移的契约。

---

## 第 9 章 新项目启动检查清单

**Day 0（写第一行业务代码之前）**

- [ ] 不变量测试套件骨架：解析解 ×n、守恒残差、极限退化、量纲断言（§2.1）
- [ ] CI 存在且绿（build + 不变量 + lint + slop 扫描）
- [ ] config schema 校验器 + passport 指纹（§2.2）
- [ ] AGENTS.md 三档边界（≤100 行）+ architecture.md 占位
- [ ] openspec/ 目录 + lessons.md 空文件

**Day 1**

- [ ] hypothesis.yaml 门禁：长计算无预注册假设不许跑（§2.3）
- [ ] 工具→null 证据强度表 v1（§3.4）
- [ ] 公式三道闸写入贡献指南（§4）
- [ ] 短门禁阶梯定义：什么跑必须先过什么（§3.5）

**Week 1**

- [ ] 第一个垂直切片功能端到端走通完整流程（propose→apply→archive→不变量全绿）
- [ ] 第一个 lessons → 机器检查升级完成（验证管道会转）
- [ ] 复杂性预算阈值进 CI（§6.5）

**持续**

- [ ] 每次纠错 → lessons.md；第二次犯 → 当天机器化
- [ ] 每个里程碑 → 独立清理 pass + lessons groom
- [ ] 每次推翻 ADR → 附反证 artifact + supersede 链

---

## 第 10 章 反模式速查

| 反模式 | 为什么是坑 | 正确做法 |
|---|---|---|
| 规则写进 md 就以为有约束 | 陈述性知识约束力≈0，AI 会亲手写下规则然后立刻违反 | 每条规则配执行钩子，配不出就不写 |
| "找出这里的问题" | 先验被设成"有错"，不足的证证据被结论接受 | "逐条证实/证伪，允许全部成立" |
| 弱仪器的 null → "不存在" | 判别力为零的测量读出二值结论，正确答案被永久删除 | 三值判决；排除需阳性对照 |
| 多变量同改的实验 | 任何结论都不可归因 | 一次一变量 + hypothesis 预注册 |
| 用代码自身输出标定测试 | 只能发现回归，无法发现系统性错误 | 期望值来自解析解/文献/已验证代码 |
| 源码保留旧版本保兼容 | 函数里几代代码并存，屎山制度化 | 黄金输出进回归测试，源码删旧路径，sunset 日期 |
| 推翻决策靠嘴 | 推翻零成本 → 频繁推翻 → 决策体系失效 | 推翻必须交反证 artifact |
| 直接跑长算例 | 参数错/不稳定 = 整轮报废 | 门禁阶梯：便宜的先跑 |
| 修三次继续修第四次 | 在错误方向上叠加代码 | 三次失败 → 停手质疑前提 |
| spec 工具装完就放心 | 默认门是建议性的，spec drift 普遍存在 | 自补强制门：无批准提案不合并 |

---

## 附录 A：hypothesis.yaml 模板

```yaml
# 长计算/实验预注册假设 —— 填不出来不许跑
hypothesis:
  id: H1
  statement: "（定量陈述假设，例：IBM 施力网格竖直错位导致桨叶相位超前）"
  expected_effect: "（预期效应量，例：相位偏移 ~30°）"
  measurement: "（诊断量，例：blade_force_azimuth vs blade_geom_azimuth）"
  noise_floor: "（噪声底与来源，例：±4°，来自 G0 三次重复）"
  resolvable: true        # 预期效应量 >> 噪声底才为 true
  positive_control: "（阳性对照，例：人为注入 z_offset=90mm，确认诊断量偏移 30±5°）"
  one_variable: "（本实验相对基线唯一改变的量）"
verdict_schema:           # 输出必须三值之一
  - confirmed             # 证实：效应观测到且超噪声底
  - falsified             # 证伪：效应不存在且阳性对照通过
  - unresolved            # 无分辨力：判别力不足
rule: |
  resolvable=false 时只允许输出 unresolved。
  排除性结论必须附阳性对照结果。
```

## 附录 B：passport.yaml 模板

```yaml
# 每次运行自动生成，写入输出目录；分析脚本先验指纹再画图
run_id: 2026-08-27T01:30:00
config_hash: sha256:...
config_path: configs/xxx.yaml
key_params:              # 影响结果的关键参数快照
  dx: 0.002
  d_p: 0.001
  rpm: { revolution: 30, far_spin: 60 }
code_commit: git:...
passport_check: |
  分析脚本开头必须:
  1. 读取本文件, 与预期 config_hash 比对, 不符则拒绝执行并报警
  2. 报告中注明 run_id 与 config_hash
```

## 附录 C：AGENTS.md 骨架（≤100 行）

```markdown
# 项目宪法
## 事实源
- specs: openspec/specs/ ；在途提案: openspec/changes/ ；决策: docs/adr/
- 架构地图: architecture.md （改结构必须同步更新）
## Always do
- 改代码后自动跑: build + 相关单测 + lint + slop 扫描，失败自己修完再报告
- 每次运行生成 passport.yaml；长计算先写 hypothesis.yaml
- 完成报告必须带验证输出，不接受"应该没问题"
## Ask first
- 改物理/数学模型、控制方程、推翻 ADR、新增依赖、改 CI/门禁本身
- 任何排除性结论（"X 不是原因"）——先给人看阳性对照
## Never do
- 禁设字段: <列出应由代码自动推导、不许手动指定的量>
- 保留 _v2/_old/注释掉的旧实现；用代码自身输出标定测试期望值
- 弱证据下"证伪"（见 §3.4 证据强度表）
## 调试纪律
- 先稳定重现再修；一次改一处；三次失败停手质疑前提
```

## 附录 D：ADR 模板

```markdown
# ADR-NNNN: 标题
status: Accepted | Superseded by ADR-MMMM
supersedes: ADR-KKKK (如有)
date: YYYY-MM-DD
## 背景与问题
## 选项与权衡
## 决定
## 依据 artifact（命令+输出/图/数据，必填）
## 推翻条件（什么证据出现时应重审本决定）
```

## 附录 E：lesson 卡格式

```markdown
## L-NNNN (date) 一句话教训
- 现象: 踩坑时的可观测表现
- 根因: 确认的根因（附 artifact）
- 误判史: 之前错误排除过什么（假阴性记录）
- 机器化: 升级为哪条断言/lint/hook/schema（第二次犯时必填）
- 状态: bank（候选）| promoted（已机器化）| stale（已过期删除）
```

---

## 最后一句实话

以上没有一条能让 AI 变可靠。它们做的是把 AI 的错误变得便宜且必然被发现。
你的目标不该是"找到一个不犯错的 agent"——这个东西不存在。
目标是让一个不可信的执行者无法造成持久损害：
**它的每个结论都带分辨率声明，每个排除都带阳性对照，每条规则都有执行钩子，每个反驳都要交证据。**
