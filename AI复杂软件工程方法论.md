# AI 辅助软件工程方法论

**Agentic Engineering Playbook v2.0 · 2026-08-27**

> **适用范围**：单人或小团队 + AI coding agent。
> v1.0 只面向"长周期、正确性敏感的复杂软件"；**v2.0 扩展为通用方法论**，
> 覆盖从一次性脚本到工业级仿真的全谱系，通过 §1 的**分级**决定用多少。
> **来源**：FHCoupler 项目数月实战教训 + 双 AI 交叉审阅事故复盘 + 2025–2026 agentic coding 生态调研。
> **本文档与具体项目无关，可直接作为新项目的启动标准。**

### v2.0 相对 v1.0 的变更

| 变更 | 说明 |
|---|---|
| **新增分级 T0/T1/T2** | v1.0 是"全有或全无"，对普通 web/app 是过度工程，会被整体丢弃。§1 |
| **核心原则通用化** | 每条原则给出**通用形式 + 三种领域实例化**（科学计算 / Web 后端 / 客户端 App）。§3 |
| **阳性对照落地为变异测试** | v1.0 的"阳性对照"在非科学计算领域无法执行；本版给出通用可执行形式。§3.3 |
| **补齐脚手架细节** | 完整目录骨架、宪法全文模板、skills 文件清单、hooks 配置、CI 分层。§4 |
| **开源生态整理成采纳表** | 按功能分组 + 采纳/观望/不用 + 理由，并加**反工具泛滥**硬约束。§5 |
| **补齐安全与供应链** | 实证显示 AGENTS.md 中 security 仅 14.8%、performance 仅 14.5% 被提及，是系统性盲区。§4.3 |
| **新增存量项目改造路径** | v1.0 全是 Day 0 绿地流程，对进行中的项目不可用。§10 |
| **新增度量与证据分级** | 怎么知道方法论生效了；文档内所有断言标注证据强度。§11、§0.5 |
| **两处降级** | "指令 150–200 条上限"与"人的时间 50/50 分配"降级为**未验证经验**，见 §0.5 |

---

## 第 0 章 总纲：四条第一性原理

全部条款由这四条推出。忘记所有细节时，记住这四句。**这四条与项目类型无关**——
Web、App、仿真、基础设施同样适用；变的只是实例化方式（§3）和执行强度（§1）。

### 0.1 机制 > 文字

**陈述性知识对行为的约束力约等于零。**

实证（本方法论的起源事故）：一个 AI 在回答结尾写下"文档里写一万条'不许编'都拦不住，
一句 `curl -sI` 三秒就拆穿。机制 > 文字"——而在写下这句话时，它已经对两个可访问的 URL
下了"存疑"判决，且没有 fetch 过任何一个。**它不仅读过那条规则，它刚刚亲手写下了那条规则，
然后在同一条消息里违反了它。**

推论：**一条规则只有在变成"不满足就跑不动"的时候才有效力。**

每条规则必须回答："**它的执行钩子是什么？**"（hook / CI gate / lint / schema / 类型系统 / 测试）。
答不出来的规则**不写**——写了是负资产，因为它制造了"已经有约束"的假象，
让人停止寻找真正的约束。

> **落地检验**：把你的 AGENTS.md 每一行拿出来问"违反它会发生什么？"
> 答案是"没什么"的行，删掉或配上钩子。

### 0.2 验证器先行

AI 生成代码的速度 ≫ 人判定对错的速度。**瓶颈在验证，不在生成。**

所以脚手架的核心不是"怎么让 AI 写得更好"，而是三件事：

1. **让验证比生成便宜** —— 快门禁先于慢门禁，确定性检查先于 LLM 审阅。
2. **让错误比正确更难通过** —— 不变量、schema、类型、三值判决挡在每一层。
3. **验证器的期望值必须来自独立于被测代码的来源** —— 解析解、规范文档、文献、
   已验证的参考实现、手工推导。**绝不能来自被测代码自身的输出。**

> **第 3 条最常被违反，且在 Web 开发里几乎是默认做法。**
> `jest --updateSnapshot`、`approvaltests` 的"接受当前输出"、
> 把生产数据库的当前状态当作期望值——**全部是用代码自身输出标定测试**。
> 这类测试**只能发现回归，永远无法发现系统性错误**：如果实现从第一天就错，测试从第一天就锁死这个错。
> 快照测试不是不能用，但它属于"变更检测器"，**不能计入正确性证据**。要在 CI 里把两类分开统计。

### 0.3 防假阴性，甚于防假阳性

AI 会把真实存在的东西判成假的（假阴性），这比凭空编造（假阳性）危险得多：

| | 假阳性（幻觉/编造） | 假阴性（错误排除） |
|---|---|---|
| 性质 | **加入**噪声 | **删除**正确信息 |
| 纠错通道 | 有——编译失败、测试失败、跑数撞墙 | **无** |
| 你会知道吗 | 会 | **不会**。正确选项永久消失，且你不知道它曾在 |
| 成本 | 一次返工 | 数十天弯路 |

**假阴性的两个产生机制：**

- **机制 A：用弱仪器测量，把 null 读成"不存在"。**
  搜索引擎对小众仓库返回空；某个工况下测不出缺陷；测试通过但根本没覆盖到那条分支。
  **仪器在"存在"和"不存在"两种情况下都返回空，判别力为零。用判别力为零的测量读出二值结论，是最贵的错误。**
- **机制 B：带着"找问题"的任务去审，于是找到了"问题"。**
  先验被设成"这里面应该有错"，不足的证据就会被结论接受。
  （对应的任务措辞规则见 §9.2。）

推论：**排除性结论的证据门槛必须高于确认性结论**（§6.3）。

### 0.4 目标不是不犯错的 agent

不存在不犯错的 agent。以上机制的目的不是让 AI 变可靠，而是：

> **把 AI 的错误变得便宜且必然被发现。**

让一个不可信的执行者无法造成持久损害：每个结论带分辨率声明、每个排除带阳性对照、
每条规则有执行钩子、每个反驳交证据。在这个体系里，错误在 30 秒内暴露，
而不是靠人恰好点开一个链接、恰好发现一个异常。

### 0.5 本文档自身的证据分级

方法论文档最常见的失败是**把口水话和实证混在一起写**，读者无法判断哪条可以据以决策。
本文档所有断言按三级标注：

| 标记 | 含义 |
|---|---|
| **【实证】** | 有可查证的论文、公开数据或本文档记录的可复现事故支撑 |
| **【实践共识】** | 社区广泛采用且机制可解释，但无对照实验 |
| **【未验证经验】** | 单方观点或未验证的经验值，**采纳前请自行验证** |

**v1.0 中两处需要降级：**

- "AGENTS.md 指令超过 150–200 条可靠性下降" → **【未验证经验】**。
  可能的来源（arXiv 2511.12884《Agent READMEs: An Empirical Study of Context Files》）
  并未给出该阈值；它给出的是内容分布：测试流程 75.9%、实现细节 70.8%、架构 68.1%、
  **security 仅 14.8%、performance 仅 14.5%**【实证】。"越短越好"方向可信，具体数字不可引用。
- "人的时间 50/50 分配（做功能 / 教系统）" → **【未验证经验】**，源自 compound engineering 的博客主张。
  方向合理，比例是修辞。

---

## 第 1 章 分级：你的项目需要多少方法论

**这是 v2.0 最重要的新增章节。** 把 T2 的全套流程套到一个 CRUD 后台上，
结果不是质量提升，是流程被整体抛弃。**分级是为了让方法论真的被执行。**

### 1.1 三个等级

| | **T0 一次性** | **T1 产品软件** | **T2 正确性敏感** |
|---|---|---|---|
| **典型** | 脚本、爬虫、demo、探索性 notebook、内部小工具 | 普通 Web 应用、移动 App、CRUD 后台、内容站、大多数 SaaS | 科学计算/仿真、支付与账务、认证授权、医疗/工控、编译器/数据库、公共 SDK |
| **错误代价** | 重跑一次 | 用户看到 bug，回滚，道歉 | 结论错误 / 资金损失 / 安全事件 / 数十天弯路 |
| **判据** | 扔掉的成本 < 做对的成本 | 有真实用户，但错误可回滚 | **错误不可回滚，或错误无法被自然发现** |
| **方法论强度** | 只用 §0 四原则 + 版本控制 | Day-0 五件套 | 全部 |

**关键判据不是"复杂不复杂"，是"错了会不会被自然发现"。**
一个复杂的前端仪表盘错了，用户会截图投诉——它是 T1。
一个简单的利息计算函数错了，可能三年没人发现——它是 T2。
**T2 的本质是缺乏自然纠错通道**，正对应 §0.3 的假阴性问题。

> **同一个项目内可以混合分级。** 更常见也更正确的做法是：整体 T1，
> 但对"钱、权限、并发、时区、迁移、外部契约"这几个子系统按 T2 处理。
> 在 AGENTS.md 里明确列出 T2 区域（见 §4.2 模板）。

### 1.2 各级必备项（可打勾）

| 措施 | T0 | T1 | T2 |
|---|:--:|:--:|:--:|
| Git + 有意义的 commit | ✅ | ✅ | ✅ |
| AGENTS.md 三档边界 | — | ✅ | ✅ |
| CI：build + lint + format + 密钥扫描 | — | ✅ | ✅ |
| 不变量/属性测试层（§3.1） | — | ✅ 轻 | ✅ 全 |
| 配置 schema 校验 | — | ✅ | ✅ |
| 运行指纹 passport（§4.5） | — | 可选 | ✅ |
| 确定性 slop 门（§8.3） | — | ✅ | ✅ |
| 依赖与供应链审计 | — | ✅ | ✅ |
| 变异测试（阳性对照，§3.3） | — | 关键模块 | ✅ 全 |
| 三值判决 + hypothesis 预注册（§6） | — | 仅性能/AB 实验 | ✅ |
| spec 状态机 + 强制门（§7.1） | — | 可选 | ✅ |
| ADR + supersede 链（§7.2） | — | ✅ 轻 | ✅ |
| 公式/关键逻辑三道闸（§3.4） | — | 仅"钱/权限/时间" | ✅ |
| lessons → 机器检查升级环（§8.5） | — | ✅ | ✅ |
| 架构适应度函数（§8.4） | — | 可选 | ✅ |

### 1.3 成本诚实交代

【实践共识】T1 全套脚手架首次搭建约 **1–2 人日**，此后每周维护约 **0.5–1 小时**。
T2 追加 **3–5 人日**（不变量层是大头）。

**什么时候不该用这套东西**：T0 项目；两周内会被扔掉的原型；
学习性项目（目的是你自己理解，不是产出可靠软件）。
**给原型套 T2 流程，是最常见的方法论浪费。**

---

## 第 2 章 六大故障模式 → 机制对照总表

| # | 故障模式 | 科学计算里的样子 | Web/App 里的样子 | 根因 | 对策机制（非文字） | 章节 |
|---|---|---|---|---|---|---|
| **F1** | 方向幻觉 | 瞎猜一个物理机制就改求解器 | 瞎猜一个 bug 原因就重构一个模块 | 无"改动提案"状态机；任务措辞诱导 | spec 状态机 + plan 批准门 + 任务措辞规则 + 三次失败停手 | §7.1, §9.2, §6.6 |
| **F2** | 参数/配置错误 | 用错 yaml 跑完整轮实验才发现 | 连错环境/用错 feature flag/迁移跑在错库上 | 配置无契约、无 provenance | config schema 校验 + 环境指纹 + dry-run 回显 | §4.5 |
| **F3** | 逻辑/公式错误 | 公式符号错、量纲错、参数编造 | 定价算错、时区差 1 天、权限判断反了、并发丢更新 | 关键逻辑无独立复算通道 | 三道闸：出处闸 + 机器闸 + 语义闸 | §3.4 |
| **F4** | 决策漂移 | ADR 被无视或无痕迹推翻 | 架构约定被绕过；同一问题三种解法并存 | 决策是被动文档；推翻零成本 | supersede 链 + 推翻必须交 artifact + 架构适应度函数 | §7.2, §8.4 |
| **F5** | 代码腐化 | 一个函数几代代码并存 | 三套 http client、四种 date 处理、`utils2.ts` | agent 迭代**单调**累积 slop；兼容策略让源码承载历史 | 确定性 slop 门 + 单一实现硬约束 + 测试承载历史 + 复杂性预算 | §8 |
| **F6** | **验证假阴性** | 正确根因被错误排除，弯路数十天 | "测试过了"但那条分支根本没被执行 | 弱仪器读二值结论；排除无门槛 | 三值判决 + 分辨率声明 + **变异测试** + 证据强度表 | §3.3, §6 |

**F6 是最危险的**：F1–F5 的错误都有机会在下游撞上现实，F6 没有。
它同时是最被忽视的——几乎所有团队都在防 F3/F5，没人防 F6。

---

## 第 3 章 核心机制的通用形式与领域实例化

**这是 v2.0 的核心章节。** v1.0 的机制全部以科学计算形态表述，
本章给出**每个机制的抽象形式**，再落到三个领域。看懂这张对应关系，
你就能把方法论移植到任何领域。

### 3.1 不变量层：什么东西改代码也不该变

**通用形式**：写业务/逻辑代码**之前**，先写出"无论实现怎么改都必须成立"的断言。
这是唯一能跨重构、跨 AI 会话存活的资产。代码会被 AI 反复改写，不变量不会。

| | 科学计算 / 仿真 | Web 后端 / SaaS | 客户端 App |
|---|---|---|---|
| **守恒律** | 质量/动量/能量残差 < 阈值 | 复式记账借贷恒等；库存不为负；转账前后总额不变 | 本地缓存与服务端最终一致；离线队列不丢不重 |
| **解析解/闭式解** | Stokes 终速、Hertz 接触、Poiseuille 流 | 从**规范文档手算**的定价/税费/折扣样例 | 从设计稿手算的布局断点、动画时长 |
| **极限退化** | 转速=0 → 场恒零；粘度→∞ → 刚体 | 空列表/单元素/权限为空 → 明确定义的行为 | 无网络/无权限/冷启动 → 明确降级路径 |
| **对称性/等价性** | 旋转镜像对称算例给对称结果 | **变形测试**：改变无关字段顺序、分页大小、时区表示，结果语义不变 | 同一操作经 UI / deeplink / 快捷方式路径结果一致 |
| **类型级断言** | 带量纲的类型，量纲不匹配=编译错误 | 分支类型（`UserId` vs `OrderId` 不可互换）、`Money{amount,currency}` 禁裸 number、状态机类型化 | 同左 + 平台差异类型化 |
| **收敛/稳定性** | 网格加密结果收敛（MMS） | 重试幂等：同一请求执行 N 次 = 执行 1 次 | 重复点击/双击提交 = 单次效果 |

**通用工具**：属性测试（property-based testing）是不变量层在普通软件里的标准载体——
Python `hypothesis`、TS/JS `fast-check`、Rust `proptest`、Java `jqwik`、Go `rapid`。
**变形测试（metamorphic testing）** 是没有 oracle 时的通用替代：
不知道正确答案是什么，但知道"输入这样变，输出应该那样变"。

> **T1 最小可行版本**（半天）：给你的 3 个核心领域对象各写 5 条属性测试，
> 加 1 条全局不变量（如"任何 API 返回的金额字段都带币种且非 NaN"）。
> 这已经比 90% 的项目强。

**验收**：空项目 CI 亮绿；任何新功能合入前必须过全套不变量。

### 3.2 三值判决：禁止二值 PASS/FAIL

**通用形式**：任何"验证/实验/排查"的结论必须三值，不许二值。

| 判决 | 含义 |
|---|---|
| **证实 confirmed** | 效应被观测到，且超过噪声底 |
| **证伪 falsified** | 效应不存在，**且有阳性对照证明本测量能测到它** |
| **无分辨力 unresolved** | 测量返回 null，但判别力不足以支撑"证伪" |

**弱证据只能支撑"无分辨力"，永远不能支撑"证伪"。**

| 领域 | 二值陷阱 | 三值化后 |
|---|---|---|
| 仿真 | "单轴工况是干净的 → 排除单轴" | 该工况下预期效应量 < 噪声底 → **无分辨力**，不许排除 |
| Web | "测试全绿 → 这个改动是安全的" | 变异测试显示该模块存活率 40% → 测试对此改动**无分辨力** |
| Web | "A/B 没显著差异 → 功能无效" | 样本量不足以检出预期效应 → **无分辨力**，不是"无效" |
| App | "我这台真机没复现 → 用户报的是假的" | 你的机型/系统版本/网络不在缺陷条件内 → **无分辨力** |
| 通用 | "搜索没找到 → 不存在" | 该检索手段对此类目标判别力为零 → **无分辨力** |

### 3.3 阳性对照：本方法论最重要的可执行化

v1.0 的"阳性对照"在科学计算里是"人为注入已知缺陷，看诊断量是否响应"。
**这个概念在普通软件里有一个成熟的、完全自动化的对应物：变异测试（mutation testing）。**

> **阳性对照的通用定义**：在下"没有问题"的结论之前，
> **先证明"如果问题存在，我这个测量会把它显示出来"。**

| 领域 | 阳性对照的执行形式 | 工具 |
|---|---|---|
| **任何有测试的代码** | **变异测试**：自动把 `>` 改成 `>=`、删掉一行、翻转布尔，看测试是否变红。存活的变异体 = 你的测试在该处判别力为零 | Stryker（JS/TS/C#/Scala）、`mutmut`/`cosmic-ray`（Python）、PIT（Java）、`cargo-mutants`（Rust）、`go-mutesting` |
| **监控/告警** | **故障注入演练**：主动制造你声称能告警的故障，确认真的响起 | Chaos 工具或手工；对每条告警至少演练一次 |
| **手工排查** | **先破坏它**：在下"X 不是原因"之前，人为把 X 弄坏，确认诊断量会变 | 无需工具，是纪律 |
| **科学计算** | 注入已知偏移/已知源项，确认诊断量按预期响应 | 项目自建 |
| **安全** | 已知漏洞样本能否被你的扫描器检出 | 基准样本集 |

**这是把 §0.3 从口号变成 CI 条目的唯一方式。**

> **T1 起步做法**：不必全仓变异测试（慢且吵）。选出"钱/权限/并发/时区"相关的
> 3–5 个模块，只对它们跑变异测试，**存活率设为 CI 门槛**（如存活率 > 20% 则失败）。
> 每晚跑，不进 PR 门（太慢）。

**规则**：任何排除性结论（"X 不是原因"、"这里没问题"）必须附阳性对照结果。
附不出来的，结论只能是"无分辨力"。

### 3.4 关键逻辑三道闸

v1.0 的"公式三道闸"是科学计算专用。通用形式是：
**任何"错了不会被自然发现"的逻辑，进入代码库前必须过三道独立的闸。**

| 闸 | 通用形式 | 科学计算 | Web/App |
|---|---|---|---|
| **G1 出处闸** | 这段逻辑的**权威来源**是什么？注释里写清楚 | 文档方程号 / 文献编号 + 推导链 | 需求文档条目号 / 法规条款 / RFC 编号 / 协议规范章节。**"产品口头说的"不算出处** |
| **G2 机器闸** | 用**独立于实现**的机器手段复算 | SymPy 独立重算 + 量纲断言 | 独立实现的对照（如用 SQL 复算业务报表、用 decimal 库复算 float 计算）、类型级约束、形式化断言（Z3/Alloy 用于权限与状态机） |
| **G3 语义闸** | 端到端的**语义正确性**验证，期望值来自 G1 的出处 | 解析极限退化 + 守恒残差 + 对称性 + MMS 收敛 | 从规范手算的黄金样例集、属性测试、变形测试、跨时区/跨币种矩阵 |

**结构性要求**：**"代码能跑"（单测/集成测试）≠ "逻辑对"（语义断言）。**
两层必须各自独立通过。这是 PhysVEC 双验证器结构的通用化。

**T1 的适用范围**：不要对全部代码上三道闸——只对下列**天然缺乏自然纠错通道**的逻辑：

- **钱**：定价、折扣、税费、汇率、分账、退款、对账
- **权限**：谁能看到什么、多租户隔离、越权路径
- **时间**：时区、夏令时、跨天边界、有效期、定时任务
- **并发**：幂等、锁、重试、事务边界、丢更新
- **迁移**：数据迁移脚本（不可逆！）、schema 变更、灰度切换
- **外部契约**：公共 API、SDK、协议实现、Webhook 语义

【实践共识】把这六类标为项目内的 T2 区域，其余按 T1，是性价比最高的分级切法。

**已知边界**：G2 在某些领域没有独立复算手段（高度抽象的张量运算、复杂 UI 交互）。
**过不了 G2 时不得静默跳过**，必须在注释和 ADR 里显式声明"此逻辑仅过 G1+G3"，
并在 §11 的度量里单独统计这类"单闸逻辑"的数量。

### 3.5 证据强度表：每个项目自建一张

**通用形式**：为项目内每种观测手段，事先写明"**null 结果意味着什么**"。
放进 AGENTS.md，让 AI 和你自己都不能把弱证据翻译成强结论。

| 观测 | null 的含义 | 强度 |
|---|---|---|
| grep / 全局搜索没找到 | 可能命名不同 / 被 ignore 排除 / 动态构造 | **弱** |
| 编译器 / 类型检查器报 undefined | 确实不存在 | **强** |
| Web 搜索没找到某仓库 | 可能未被索引（小众/新建） | **弱** |
| 直接访问 URL 返回 404 | 确实不存在 | **强** |
| 单元测试全绿 | **可能根本没覆盖到该分支** | **弱**（除非有变异测试背书） |
| 变异测试存活率 < 5% | 测试确有判别力 | **强** |
| 覆盖率 90% | 行被执行过 ≠ 断言检查过 | **弱** |
| 本地/单机没复现 | 环境、并发度、数据规模不同 | **弱** |
| 日志里没有该错误 | 可能没打日志 / 被采样丢弃 / 被吞异常 | **弱** |
| 输出文件里看不到某字段 | 可能被输出掩码/序列化过滤 | **弱** |
| 守恒律/业务不变量残差达标 | 该量确实守恒 | **强** |
| 短算例/短压测没崩 | 长时可能仍崩（延迟触发、泄漏累积） | **弱** |
| 静态分析无告警 | 该规则可能未启用 / 有 suppress | **弱** |

**规则：弱证据行的 null，永远不许翻译成"不存在 / 无问题 / 已排除"。**

---

## 第 4 章 脚手架：具体建什么文件

本章回答"新项目开工第一天到底动手做什么"。给出**完整目录骨架 + 每个文件的内容模板**。

### 4.1 目录骨架

```
project/
├── AGENTS.md                    # 宪法（≤100 行，永远在上下文里）
├── architecture.md              # 活架构图：模块地图/数据流/热点/技术债
├── CONTRIBUTING.md              # 人看的；AI 看 AGENTS.md
├── .claude/
│   ├── settings.json            # hooks + 权限（团队共享，入库）
│   ├── settings.local.json      # 个人覆盖（gitignore）
│   └── skills/                  # 按需加载的方法论（§4.4）
│       ├── verify-claims/SKILL.md
│       ├── root-cause-debug/SKILL.md
│       ├── propose-change/SKILL.md
│       ├── write-adr/SKILL.md
│       ├── cleanup-pass/SKILL.md
│       ├── promote-lesson/SKILL.md
│       └── (T2) run-verdict/SKILL.md
│       └── (T2) critical-logic-gate/SKILL.md
├── docs/
│   ├── adr/                     # ADR-0001.md ... 带 supersede 链
│   └── lessons.md               # 教训银行 → 机器检查升级环
├── openspec/                    # (T2/可选 T1) 变更状态机
│   ├── specs/                   # 当前真相
│   ├── changes/<name>/          # 在途提案
│   └── changes/archive/
├── tests/
│   ├── invariants/              # §3.1 不变量层 —— 最重要的目录
│   ├── golden/                  # 期望值来自规范/手算，非代码输出
│   ├── property/                # 属性测试 / 变形测试
│   └── snapshot/                # 变更检测器（不计入正确性证据，单独统计）
└── scripts/
    ├── preflight.py             # 配置/环境校验 + 指纹
    └── check_evidence.py        # (T2) 判决三值 schema 校验
```

### 4.2 宪法 `AGENTS.md`（完整模板，直接改用）

**设计原则**：宪法**永远在上下文里**，所以必须短；细节全部下沉到 skills（按需加载）
和 reference 文档（指针）。**每一行都必须有执行钩子，否则删掉**（§0.1）。

```markdown
# 项目宪法

## 0. 事实源（冲突时以此为准）
- 需求/规范: openspec/specs/   在途提案: openspec/changes/
- 决策记录: docs/adr/（带 supersedes 链）
- 架构地图: architecture.md（改变模块边界必须同步更新）
- 教训: docs/lessons.md
> 冲突时优先级: ADR > specs > architecture.md > 代码注释 > 本文件其余部分

## 1. T2 区域（本项目内错误不可自然发现的部分）
以下路径的改动按最高强度执行（三道闸 §G1-G3 + 阳性对照 + ADR）：
- src/billing/**      金额、税费、对账
- src/auth/**         权限判定、多租户隔离
- migrations/**       不可逆数据变更
- src/api/public/**   外部契约
其余路径按常规强度。

## 2. Always do（不用问）
- 改代码后自动跑: format + lint + 受影响单测 + 密钥扫描；失败自己修完再报告
- 完成报告必须附验证输出（命令 + 结果）。不接受"应该没问题""理论上可以"
- 新增业务对象时同步添加属性测试（tests/property/）
- 报告 bug 修复时必须说明: 复现步骤 + 根因 + 为什么这个修复能防止再发

## 3. Ask first（先问再做）
- 改 T2 区域的任何逻辑
- 新增/升级依赖（供应链风险）
- 推翻或修改任何 ADR
- 改 CI、hooks、门禁本身（改裁判需要授权）
- 任何数据迁移、schema 变更、删除数据的操作
- 任何排除性结论（"X 不是原因""这里没问题"）——先出示阳性对照

## 4. Never do
- 保留同一功能的多个版本（_v2 / _old / 注释掉的旧实现）。删掉，历史交给 git 和回归测试
- 用代码自身输出标定测试期望值（含 --updateSnapshot 当作修复手段）
- 在弱证据下给出"证伪/已排除"结论（见 §6 证据强度表）
- 提交密钥、令牌、真实用户数据
- 吞掉异常（空 catch / 裸 except / `_ = err`）
- 为了让测试通过而修改测试，除非能说明测试原本就错在哪

## 5. 证据强度表（null 结果的含义）
| 观测 | null 含义 | 强度 |
|---|---|---|
| 测试全绿 | 可能没覆盖到该分支 | 弱 |
| 变异测试存活率<5% | 测试确有判别力 | 强 |
| 本地没复现 | 环境/并发/数据规模不同 | 弱 |
| grep 没找到 | 命名不同/动态构造 | 弱 |
| 类型检查报错 | 确实不存在 | 强 |
（每次踩坑后追加一行）

## 6. 调试纪律
- 先稳定重现再动手；一次只改一处验证一个假说
- 假说失败必须撤回改动，不许在错误方向上继续叠代码
- **修三次还没好 → 停手，质疑架构/前提本身，不许尝试第四次**

## 7. 安全与性能底线（默认易被忽略，故显式列出）
- 一切外部输入视为不可信；SQL 一律参数化；输出按上下文转义
- 认证与授权分离；越权检查在服务端，不在前端
- 新增 N+1 查询、无界循环、无分页列表接口 → 必须在 PR 说明里标注
- 依赖变更必须附审计结果
```

> **为什么显式写第 7 节**：【实证】对 2500+ 仓库上下文文件的研究显示，
> 测试流程被提及 75.9%、实现细节 70.8%、架构 68.1%，而 **security 仅 14.8%、performance 仅 14.5%**。
> 这是全行业系统性盲区——AI 不会主动补，因为大多数样本里就没有。

### 4.3 三档边界的判定标准

很多人写不好三档，是因为没有判据。用这个：

| 档位 | 判据 | 反例（常见错误分档） |
|---|---|---|
| **Always** | 做错了**立刻可见且可回滚**，且执行成本低 | 把"重构模块"放这里 ❌——不可立刻验证 |
| **Ask first** | **不可逆** 或 **难以验证** 或 **影响契约/裁判** | 把"改 CI 门禁"放 Always ❌——等于让被考核者改考卷 |
| **Never** | 无论理由多充分都不该做；违反必然产生长期负债 | 把"不许写 bug"放这里 ❌——不可执行，是废话 |

**Never 档的检验标准：能不能配一条 lint/CI 规则？** 配不出的，说明它不是 Never，是愿望。

### 4.4 Skills：按需加载的方法论

**为什么用 skills 而不是全写进 AGENTS.md**：
【实证】skills 采用三层渐进披露——启动时只加载 name+description（约 100 tokens/skill），
触发时才加载正文（建议 < 5000 tokens），引用文件按需再加载。
**这让你可以拥有一本厚方法论，而不污染每次对话的上下文。**

`SKILL.md` 的必需结构：YAML frontmatter 只需 `name` 和 `description` 两个字段 + Markdown 正文。
**`description` 决定它何时被触发，必须同时写清"做什么"和"什么时候用"。**

**推荐的 skills 清单（按优先级）：**

| Skill | description 要点（决定触发时机） | 正文核心 | 级别 |
|---|---|---|---|
| **verify-claims** | "逐条核实一组断言/一份他人给的结论/一段调研结果时使用。产出证实或证伪的 artifact" | §9.2 措辞规则；每条断言必须给命令+输出；**显式允许输出"全部成立"**；禁止在无 artifact 时给否定结论；工具选择表（搜索 vs 直接访问） | 全部 |
| **root-cause-debug** | "排查 bug、异常、测试失败、性能问题时使用。先根因后修复" | 稳定重现 → 边界日志二分 → 单假说单改动 → **三次失败停手质疑前提**；禁止症状修复；排除性假说需阳性对照 | 全部 |
| **propose-change** | "开始任何非平凡功能改动前使用，产出提案供人批准" | 先探讨不写码 → proposal.md（动机/选项/权衡/影响面/回滚方案）→ tasks 拆分（每个任务带确切文件路径和验证命令）→ 等批准 | T1/T2 |
| **write-adr** | "做出或推翻架构决策时使用" | ADR 模板（附录 D）；**推翻必须附反证 artifact**；维护 supersedes 链；写明"推翻条件" | T1/T2 |
| **cleanup-pass** | "里程碑结束、或代码明显腐化时，单独运行的清理任务" | 唯一目标：测试保持绿、消灭重复/死代码/命名不一致、**不改行为**；禁止顺手加功能；产出 diff 摘要 | T1/T2 |
| **promote-lesson** | "同一个错误第二次发生时使用，把教训升级为机器检查" | 判断可机器化形式（断言/lint/schema/hook/测试）→ 实现 → 验证它能拦住原始错误（**这本身是阳性对照**）→ 更新 lessons.md 状态 | T1/T2 |
| **run-verdict** | "运行长时实验/性能测试/AB 测试/仿真判决前后使用" | hypothesis 预注册模板；三值判决；分辨率计算；阳性对照要求 | T2 |
| **critical-logic-gate** | "在 T2 区域（钱/权限/时间/并发/迁移/外部契约）编写或修改逻辑时使用" | 三道闸 G1/G2/G3 检查表；出处必须可引用；独立复算方法清单；黄金样例必须手算 | T2 |

**写 skill 的两条铁律：**
1. **description 里必须有"什么时候用"**，否则永远不会被触发——这是 skill 失效的头号原因。
2. **正文写"怎么做"的步骤，不写"要重视"的态度。** 态度不可执行（§0.1）。

### 4.5 Hooks：真正的强制力所在

**AGENTS.md 是建议，hooks 是法律。** 最小可用集：

| 时机 | 做什么 | 级别 |
|---|---|---|
| **PostToolUse**（Edit/Write 后） | 对改动文件跑：formatter + 快速 lint + slop 扫描。**几秒内**，失败信息回灌给 agent 自己修 | T1/T2 |
| **PreToolUse**（Bash 前） | 拦截危险命令：`git push --force`、`rm -rf`、直连生产库、`--updateSnapshot`；拦截对 T2 路径和门禁文件的改动（要求先问） | T1/T2 |
| **PreToolUse**（长任务前） | 无 `hypothesis.yaml` 不许启动超阈值计算/压测 | T2 |
| **Stop**（会话结束前） | 跑一次完整快测 + 未提交变更提示；报告本次会话是否引入未记录的架构变更 | T1/T2 |

**性能纪律**【实践共识】：每次编辑触发的 hook 必须 **< 3 秒**，否则会拖垮整个工作流。
昂贵检查放 Stop 或 CI。

**配置位置**：`.claude/settings.json`（团队共享，入库）；个人覆盖放 `settings.local.json`（gitignore）。

### 4.6 配置契约与运行指纹

**F2 的唯一根治法**，也是全部措施里最便宜的（半天）。

1. **配置 schema 校验**：加载时机械拒绝——未知键报错、类型检查、范围检查、
   跨字段一致性断言。项目每个已知坑写成一条断言。
   工具：Python `pydantic`、TS `zod`、通用 JSON Schema。
   **关键**：未知键必须**报错**，不能忽略——拼错的配置项静默失效是经典事故。
2. **运行指纹 passport**：每次运行把 `config hash + 关键参数 + code commit + 环境标识`
   写进输出/日志；所有分析脚本、报表、结论**先校验指纹再使用**。
   Web 场景的对应物：每个部署带 build ID + config 快照；每份数据报表标注生成时的代码版本与查询参数。

**验收**：故意改错一个参数 → 运行被拒绝并指出具体字段；输出里一定能追溯到配置来源。

### 4.7 CI 分层

| 层 | 何时 | 内容 | 时限 |
|---|---|---|---|
| **L0 本地 hook** | 每次编辑 | format + 快 lint + slop 扫描（仅改动文件） | < 3s |
| **L1 pre-commit** | 每次提交 | 上述 + 密钥扫描 + 受影响单测 | < 30s |
| **L2 PR 门** | 每个 PR | 全量单测 + **不变量层** + 类型检查 + 架构适应度 + 依赖审计 + slop 分数门 | < 10min |
| **L3 每晚** | nightly | **变异测试** + 大预算属性测试 + e2e + 性能基线 + 长时/收敛回归 | 不限 |

**分层的意义**：把慢检查放进 PR 门会让人绕过 CI（`--no-verify` 成为习惯），
方法论就此崩塌。**门禁的第一设计目标是"不被绕过"，第二才是"严格"。**

---

## 第 5 章 开源生态整理与采纳决策

**先说硬约束（比工具本身更重要）：**

> ### 🚫 反工具泛滥硬约束
> **单人/小团队项目，外部工具总数 ≤ 4 个。**
> 每引入一个工具，问三个问题：
> 1. 它替代了哪条我现在靠自觉执行的规则？（答不出 → 不引入）
> 2. 它坏了/停止维护，我的流程会不会瘫痪？（会 → 不引入或做好剥离方案）
> 3. 它的维护成本 < 它拦住的错误成本吗？
>
> **工具越多，实际执行的越少。** 一个真正跑起来的 lint 规则，胜过五个装好没配的框架。

### 5.1 按功能分组的采纳表

**图例**：✅ 建议采纳 · 🟡 观望/特定场景 · ⛔ 不建议（对单人小队）

#### A. 规范与变更治理

| 项目 | 是什么 | 判断 |
|---|---|---|
| [OpenSpec](https://github.com/Fission-AI/OpenSpec) | `proposal → apply → archive` 三段状态机，`specs/`（真相）与 `changes/`（提案）物理隔离 | ✅ **T2 首选**。专为**已有代码库演进**设计。⚠️ 默认门是建议性的，必须自补强制门（§7.1） |
| [GitHub Spec Kit](https://github.com/github/spec-kit) | `specify → plan → tasks → implement` 四阶段闸门 | 🟡 偏绿地新项目；已跑起来的项目迁移成本高 |
| BMAD-METHOD | 多角色 agent 全 SDLC 编排 | ⛔ 对单人过重 |
| Agent OS v3 | 已放弃"spec 作为真相源"，退回 Plan Mode | ⛔ 与本方法论核心冲突 |
| [AgDR](https://github.com/me2resh/agent-decision-record) | Agent 决策记录标准，扩展 ADR 到 AI 决策；带 schema | ✅ 轻量，思路可直接抄进 `docs/adr/` 而不必引入工具 |
| [adrkit](https://github.com/mbeacom/adrkit) | 机器可读 ADR，带 lint/CI/MCP，PR 上浮现受影响决策 | 🟡 团队协作时价值大，单人可先手写 |

#### B. 方法论 Skills

| 项目 | 是什么 | 判断 |
|---|---|---|
| [obra/superpowers](https://github.com/obra/superpowers) | 最流行的 Claude Code 方法论 skill 集：brainstorming、TDD、systematic-debugging、verification-before-completion、subagent 开发、写 skill | ✅ **强烈建议**。`systematic-debugging`（三次失败停手）与本方法论 §6.6 完全一致，可直接用而不必自己写 |
| [Plannotator](https://github.com/backnotprop/plannotator) | 挂 ExitPlanMode hook，把计划弹到本地 Web UI 逐行批注/驳回/批准 | ✅ **F1 的直接对策**。把"计划批准门"从纪律变成机制 |

#### C. 代码防腐

| 项目 | 是什么 | 判断 |
|---|---|---|
| [aislop](https://github.com/scanaislop/aislop) | 50+ 确定性规则（**运行时不用 LLM**）：死代码、吞异常、重复 helper、幻觉 import、超大函数、`as any`；CLI/CI/**post-edit hook**；支持 TS/JS/Python/Go/Rust/Java 系/C/C++ | ✅ **首选**。确定性、快、可进 hook。多语言覆盖广 |
| [Topos](https://github.com/Krv-Labs/topos) | 结构化质量评分 GOLD/SILVER/BRONZE/SLOP，MCP 暴露给 agent 自我迭代 | 🟡 思路好（给 agent 可测量目标），项目较新，先观望 |
| SonarQube（architecture-as-code） | 架构边界即代码，漂移像漏洞一样 block PR；认知复杂度评分 | 🟡 有团队/服务器时值得；单人偏重 |
| ArchUnit / import-linter / dependency-cruiser | **架构适应度函数**：模块依赖规则写成测试 | ✅ 轻量、语言原生（Java/Python/JS），**F4 的直接对策** |

#### D. 验证与判别力（本方法论最看重）

| 类别 | 工具 | 判断 |
|---|---|---|
| **变异测试**（阳性对照） | Stryker（JS/TS/C#）、`mutmut`/`cosmic-ray`（Py）、PIT（Java）、`cargo-mutants`（Rust） | ✅ **T1/T2 都该有**。这是 §0.3 唯一的可执行化形式 |
| **属性测试**（不变量层） | `hypothesis`（Py）、`fast-check`（TS/JS）、`proptest`（Rust）、`jqwik`（Java） | ✅ 不变量层的标准载体 |
| **形式化**（G2 机器闸） | Z3 / Alloy / TLA+ | 🟡 仅用于权限模型、状态机、并发协议这类**高价值小规模**问题 |

#### E. 符号与公式（T2 科学计算专用）

| 项目 | 是什么 | 判断 |
|---|---|---|
| [symkit-mcp](https://github.com/LBurny/symkit-mcp) | MCP server，41 个工具：分步符号推导、**每步带 provenance 可回滚**、量纲一致性验证、导出 Python/LaTeX/SymPy | ✅ **最贴合 G2 机器闸**。⚠️ 项目很新（~10 star），需自评代码质量与维护状况 |
| [sdiehl/sympy-mcp](https://github.com/sdiehl/sympy-mcp) | 把 SymPy 能力暴露为 MCP | ✅ 更成熟、更简单的替代；无 provenance 链 |
| [QWED](https://github.com/QWED-AI/qwed-mcp) | 确定性验证网关：SymPy 查算式 / Z3 查逻辑 / AST 查代码；LLM 只做翻译不做裁判 | 🟡 理念完全正确（"不让 LLM 检查自己"），项目新，先借鉴理念 |

> **本表的一个教训**：`symkit-mcp` 曾在本方法论的起源事故中被一个 AI 判为"编造的仓库"，
> 依据是"搜索引擎搜不到"。它真实存在。**这正是 §0.3 机制 A 的实例，
> 也是为什么 §3.5 的证据强度表把"搜索没找到"列为弱证据、把"URL 404"列为强证据。**

#### F. 安全与供应链（普遍缺失，见 §4.2 第 7 节）

| 类别 | 工具 | 判断 |
|---|---|---|
| 密钥泄露扫描 | `gitleaks`、`trufflehog` | ✅ 必备，进 pre-commit |
| 依赖漏洞审计 | `npm audit` / `pip-audit` / `cargo audit` / `govulncheck` | ✅ 必备，进 PR 门 |
| 依赖更新 | Dependabot / Renovate | ✅ 但配置成**批量+定期**，否则 PR 噪声淹没真信号 |
| SAST | Semgrep | 🟡 规则集需调，否则误报吞噬注意力 |

#### G. 证据来源（不是工具，是引用时的依据）

| 来源 | 用途 |
|---|---|
| [SlopCodeBench](https://arxiv.org/abs/2603.24755) | 【实证】agent 在长周期迭代中**单调**累积冗余与结构侵蚀；11 个模型无一端到端解决任何问题 |
| [GitClear 2026 维护性报告](https://www.gitclear.com/the_ai_code_quality_maintainability_gap) | 【实证】copy-paste 占比 9.4%(2022) → 15.7%(2026H1)；moved（重构）占比降至 3.8% |
| [GitHub: 2500+ 仓库 AGENTS.md 研究](https://github.blog/ai-and-ml/github-copilot/how-to-write-a-great-agents-md-lessons-from-over-2500-repositories/) | 【实证】三档边界（Always/Ask first/Never）是最能防灾难性错误的一节 |
| [arXiv 2511.12884 Agent READMEs](https://arxiv.org/abs/2511.12884) | 【实证】security 14.8% / performance 14.5% 的系统性盲区 |
| [HELIOS (arXiv 2607.24051)](https://arxiv.org/abs/2607.24051) | 【实证】符号推导 → SymPyVerifier 复核 → 失败打回（≤3 轮）→ 生成 C++ 求解器的完整链路 |
| [TPBench 符号弱验证器 (arXiv 2506.20729)](https://arxiv.org/abs/2506.20729) | 【实证】SymPy 增强的逐步验证在物理题上带来最高 22% 提升 |

### 5.2 最小推荐组合

| 级别 | 组合（≤4 个外部工具） |
|---|---|
| **T1（Web/App）** | `aislop`（防腐 hook）+ 属性测试库 + 变异测试（仅关键模块）+ `gitleaks`。**其余全部自建**：AGENTS.md、skills、hooks、CI 分层 |
| **T2（正确性敏感）** | 上述 + OpenSpec（带自补强制门）+ superpowers + 领域符号工具（如 symkit-mcp / sympy-mcp） |

**注意：自建部分才是主体。** 上面的工具只解决"确定性扫描"和"判别力测量"，
方法论的核心（三值判决、阳性对照纪律、三档边界、教训升级环）没有现成工具，
也不需要——它们是几十行脚本 + 几个 hook + 一份 100 行的宪法。

---

## 第 6 章 判决与实验纪律

适用于：T2 全部；T1 的性能实验、A/B 测试、线上问题排查。

### 6.1 三值判决 schema
见 §3.2。**禁止二值 PASS/FAIL。**

### 6.2 预注册 hypothesis（先写后跑）
超过阈值成本的实验（T2：> 1 小时计算；T1：任何 A/B 或压测），
必须先存在已批准的 `hypothesis.yaml`（附录 A），否则 hook 拦截。

**为什么要预注册**：仅要求"交 artifact"是可以被钻空子的——
AI（和人）能为一个稻草人断言生产漂亮的证据。
**先写假设、预期效应量、阳性对照，人批准，再跑**，堵上这个口子。
这也是"一次只改一个变量"的天然载体。

### 6.3 排除性结论的高门槛

- **"X 是根因"** → 需要一次正面演示。
- **"X 不是根因"** → 需要阳性对照（§3.3）：先证明"如果 X 真坏了，这个诊断量会变"。

**每次下排除结论前自问："如果 X 真的存在，我这个测量会找到它吗？"**
答不上来，结论只能是"无分辨力"。

### 6.4 证据强度表
见 §3.5，每个项目自建并持续追加。

### 6.5 一次一变量 + 门禁阶梯

- **一次只改一个变量。** 多变量同改的实验不产生任何可归因结论——它的信息量是零，
  但会产生"我们试过了"的错觉，比不做更糟。
- **便宜的门先于贵的跑。** 任何昂贵操作前必须过一道快门禁。
  科学计算：短算例 → 中算例 → 生产算例。
  Web：本地 → 预览环境 → 灰度 1% → 全量。**阶梯要制度化，不靠自觉。**

### 6.6 三次失败停手

1. 先根因调查：完整读错误、打边界日志、**必须能稳定重现**才许动手修。
2. 一次改一处验证一个假说；**假说失败必须撤回改动**，不许在错误方向上继续叠加代码。
3. **修了三次还没好 → 强制停下来质疑架构/前提本身**，而不是尝试第四次。

**症状修复即失败。** 这条与 superpowers 的 `systematic-debugging` 一致，可直接复用该 skill。

---

## 第 7 章 变更与决策治理

### 7.1 spec 状态机（T2；T1 可用简化版）

```
openspec/
├── specs/            # 当前真相（唯一事实源）
├── changes/<name>/   # 在途提案: proposal.md + design.md + tasks.md + spec deltas
└── changes/archive/  # 已完成（日期前缀，永久留痕）
```

流程：`explore（不写码的探讨）→ propose（人批准）→ apply（按 tasks 实施）→ archive（delta 合并回 specs）`

> **关键补丁——自补强制门**：此类工具的默认门是**建议性的**（validator 只查结构不查行为，
> spec drift 是社区普遍抱怨）。必须自行补硬门：
> **CI 检查"功能性改动的 PR 是否存在已批准的 proposal"，否则拒绝合并。**
> 不补这个门，状态机会退化成装饰目录。

**T1 简化版**：不引入 OpenSpec，只要求非平凡改动先在 PR 描述里写清
"动机 / 选项 / 选择理由 / 影响面 / 回滚方案"四段，并配 `propose-change` skill 生成。

### 7.2 决策记录与推翻纪律

- 决策用轻量 ADR（附录 D），带 `supersedes` / `superseded-by` 链。
- **推翻 ADR 是合法且必要的操作**——但**必须交 artifact**：反证的命令 + 输出 + 数据/图。
  **不接受纯论证。** 免费打出"存疑"两个字是被禁止的：
  要推翻的人一旦被迫执行命令，往往就会看到真相（这正是起源事故的教训）。
- **ADR 频繁被推翻时，修上游别修下游。** 推翻频率高通常不是 ADR 制度的问题，
  而是**喂给 ADR 的判决质量不够**。把判决三值化（§6）之后，推翻频率会自然下降。
- **冲突处理**：ADR 与当前计划冲突时，要么遵守 ADR，要么起草继任 ADR——**不许静默偏离**。
  静默偏离是 F4 的完整形态：决策还在文档里，代码已经不认它了。
- 每份 ADR 必填 **"推翻条件"**：什么证据出现时应重审本决定。
  这让推翻从"打脸"变成"按预案执行"，大幅降低推翻的心理成本和随意性。

---

## 第 8 章 代码防腐

【实证】agent 迭代会**单调**累积 slop（SlopCodeBench）；
行业数据：copy-paste 占比 2022→2026H1 从 9.4% 升到 15.7%，moved（重构）占比降至 3.8%（GitClear）。
**这是行业性问题，不是你的能力问题。防腐靠机制，不靠提醒。**

1. **单一实现硬约束**：禁止同一功能多版本并存（`_v2` / `_old` / 注释保留 / `utils2.ts`）。
   lint + CI 强制，不写进 md 靠自觉。
2. **测试承载历史，源码不承载历史**：
   "保留旧行为以保证向后兼容/历史结果可复现"是**合理需求**，但正确做法是
   **旧行为固化成回归测试的黄金输出，然后从源码删除旧路径**。
   任何 legacy 兼容分支必须带 **sunset 日期**，到期迁移或删除（strangler pattern）。
   > 这一条治的是**制度性**腐化：很多项目的屎山不是 AI 造成的，
   > 是"永不删除旧行为"的兼容策略造成的，AI 只是加速器。
3. **确定性 slop 门**：无 LLM 的确定性扫描（aislop 类），post-edit hook 即时回灌，CI 设分数下限。
4. **独立清理 pass**：实现循环里的"重构"步骤**必然**被 agent 缩水成改个变量名。
   每个里程碑跑一次**独立上下文**的清理任务，唯一目标：测试保持绿、消灭重复/死代码/命名不一致、**不改行为**。
5. **复杂性预算**：函数行数 / 文件行数 / 圈复杂度 / 嵌套深度阈值进 CI，超阈触发人工评审。
   "一个函数里 30 个 if"不是风格问题，是下一代 bug 的孵化器。
6. **架构适应度函数**：模块依赖规则写成可执行测试（ArchUnit / import-linter / dependency-cruiser）。
   这是 F4 在代码层的对策——**架构约定必须能失败**，否则它只是愿望。

---

## 第 9 章 人机分工与多 AI 协作

### 9.1 人的角色

人应该把时间花在**定义"什么算对"**（不变量、spec、门禁、hypothesis 审批）
和**让系统变聪明**（教训升级、规则维护）上，而不是 review 每一行代码。
**AI 是高速生产者，人是架构师兼守门员。**

（v1.0 的"50/50 时间分配"是【未验证经验】，方向合理，比例不必当真。）

### 9.2 任务措辞规则（防机制 B）

| | 措辞 | 后果 |
|---|---|---|
| ❌ | "找出这段代码/这个结果的问题" | 它**会**找到问题，不管问题是否存在。先验被设成"有错" |
| ❌ | "这个方案有什么风险？" | 同上，会生产风险清单 |
| ✅ | "对下面每一条断言，给出证实或证伪的 artifact（命令+输出）。**允许并鼓励输出'全部成立'**" | 结论由证据决定 |
| ✅ | "列出你**验证过**的和你**没验证**的，分开写" | 强制暴露不确定性 |

**必须显式地、低成本地给出"没问题"这个出口。**
否则你得到的是为交差而生产的问题清单——**这正是"AI 瞎想一个点然后按这个方向做"的上游成因**。
F1 的根不在 AI 的想象力，在你的提问方式。

### 9.3 多 AI 交叉审阅规则

不同模型的失败模式确实不重合，互审有价值。但：

- **裁决权归 artifact，不归修辞。**
  【实证，起源事故】没有 artifact 约束的交叉审阅，会让**错误的一方看起来更权威**——
  因为审阅者天然占据"我在检查"的位置，而"我核查过了"这个动作本身会成为信任来源，
  与核查结果无关。
- 审阅方每一条否定意见必须附**可复现的命令和输出**；附不出来的，不进入结论。
- 审阅任务同样遵守 §9.2：是"逐条证实/证伪"，不是"找茬"。
- **被审阅方不必立刻认错。** 收到否定意见时，第一动作是要求 artifact，不是修改。

### 9.4 长任务工程

- 大任务拆成**带确切文件路径和验证命令**的小任务；每个任务用新鲜子上下文执行（防上下文漂移）。
- 计划生成后设**人批准门**（Plannotator 可把这一步机制化），再进入实施。
- 并行多 agent 时，ADR/spec 是防止各自漂移的**契约**。
- 反压内环：改完自动跑检查、失败自己修，人只看意图层。
  **"成功输出压缩成一行，失败输出完整可行动信息"**——别把一千行日志倒进上下文。

---

## 第 10 章 存量项目改造路径

v1.0 全是 Day-0 绿地流程，对进行中的项目不可用。存量改造的第一原则是：

> **不要中途更换全流程框架。** 已跑数月的项目引入 spec-kit / BMAD 这类全流程框架，
> 迁移成本通常高于它省下的成本。**先加门禁，后加流程。**

**推荐顺序（每步独立见效，可随时停）：**

| 阶段 | 做什么 | 工期 | 见效 |
|---|---|---|---|
| **1** | 配置 schema 校验 + 运行指纹（§4.6） | 半天 | 立刻停止"跑完才发现参数错" |
| **2** | `aislop` 类 post-edit hook + 密钥扫描 | 半天 | 腐化速度立刻下降 |
| **3** | 写 AGENTS.md（三档边界 + T2 区域 + 证据强度表初版） | 半天 | 减少方向性事故 |
| **4** | 对**最近三次事故**涉及的模块补不变量/属性测试 | 1–2 天 | 用真实历史选打击点，命中率最高 |
| **5** | 对 T2 区域跑一次变异测试，看存活率 | 1 天 | **通常是最震撼的一步**——你会发现"绿的测试"判别力极低 |
| **6** | 建 `lessons.md` + `promote-lesson` skill；把历史教训回填 | 1 天 | 停止重复踩坑 |
| **7** | ADR 补 supersede 链 + "推翻条件"字段；旧决策不必补全，从现在开始 | 持续 | 决策停止漂移 |
| **8** | （仅 T2）spec 状态机 + 强制门 | 2–3 天 | 变更可追溯 |

**第 4 步的选点方法**：翻你的 issue/事故记录，找出**同一类错误出现 ≥2 次**的模块。
不要凭感觉选"重要模块"——**用历史数据选，不用直觉选**。

**不要做的事**：
- 不要为了上方法论而大规模重构（那是 F1 的完美案例）。
- 不要一次性补全所有历史 ADR/lessons，只从现在开始。
- 不要在同一周引入超过一个新工具，否则无法归因是哪个起了作用（§6.5 一次一变量同样适用于流程改造）。

---

## 第 11 章 度量：怎么知道这套东西在起作用

**没有度量的方法论会退化成 cargo cult。** 每月看一次这五个数，5 分钟：

| 指标 | 怎么测 | 健康方向 | 说明 |
|---|---|---|---|
| **返工率** | "修复某问题的 commit 又被后续 commit 修改"的比例 | ↓ | 直接反映根因质量 |
| **判决推翻率** | 被后续证据推翻的结论 / 全部结论 | ↓ | §6 生效时应显著下降 |
| **平均发现时延** | 缺陷引入 → 被发现的间隔（git blame 可算） | ↓ | 这是 §0.4"错误便宜且必然被发现"的直接度量 |
| **变异测试存活率** | 工具直接给出 | ↓ | 唯一能证明测试有判别力的数 |
| **教训机器化率** | promoted / (promoted + bank) | ↑ | 升级环是否真的在转 |

**反指标（不要追）**：代码行数、测试覆盖率、commit 数量、PR 数量。
覆盖率尤其危险：**行被执行过 ≠ 断言检查过**，高覆盖率 + 高变异存活率是典型的假安全感。

---

## 第 12 章 反模式速查

| 反模式 | 为什么是坑 | 正确做法 |
|---|---|---|
| 规则写进 md 就以为有约束 | 陈述性知识约束力≈0；AI 会亲手写下规则然后立刻违反 | 每条规则配执行钩子，配不出就不写 |
| "找出这里的问题" | 先验被设成"有错"，不足的证据被结论接受 | "逐条证实/证伪，允许全部成立" |
| 弱仪器的 null → "不存在" | 判别力为零的测量读出二值结论，正确答案被永久删除 | 三值判决；排除需阳性对照 |
| "测试全绿所以安全" | 覆盖 ≠ 判别力；测试可能根本没走那条分支 | 变异测试；存活率进 CI |
| `--updateSnapshot` 当修复手段 | 用代码自身输出标定期望值，锁死错误 | 期望值来自规范/手算/独立实现 |
| 多变量同改的实验 | 结论不可归因，还制造"试过了"的错觉 | 一次一变量 + 预注册 |
| 源码保留旧版本保兼容 | 屎山制度化 | 黄金输出进回归测试，源码删旧路径 + sunset 日期 |
| 推翻决策靠嘴 | 推翻零成本 → 频繁推翻 → 决策体系失效 | 推翻必须交反证 artifact |
| 直接跑昂贵操作 | 参数错/不稳定 = 整轮报废 | 门禁阶梯：便宜的先跑 |
| 修三次继续修第四次 | 在错误方向上叠加代码 | 三次失败 → 停手质疑前提 |
| spec 工具装完就放心 | 默认门是建议性的，spec drift 普遍 | 自补强制门：无批准提案不合并 |
| 给原型套 T2 流程 | 方法论浪费，最终整体被抛弃 | 先分级（§1）再选强度 |
| 一次引入五个工具 | 无法归因，且实际执行的更少 | ≤4 个工具，一周一个 |
| 把慢检查放进 PR 门 | 人开始 `--no-verify`，方法论崩塌 | CI 分层；门禁第一目标是不被绕过 |
| 追覆盖率 | 高覆盖 + 高变异存活 = 假安全感 | 追变异存活率与发现时延 |

---

## 附录 A：hypothesis.yaml 模板

```yaml
# 昂贵实验/长计算/AB 测试 预注册 —— 填不出来不许跑
hypothesis:
  id: H1
  statement: "（定量陈述，例：新缓存层能把 p99 从 800ms 降到 300ms 以下）"
  expected_effect: "（预期效应量，定量。不允许'应该会变好'）"
  measurement: "（诊断量，例：p99_latency，来源 Prometheus xxx 指标）"
  noise_floor: "（噪声底与来源，例：±60ms，来自基线连续 3 天同时段方差）"
  resolvable: true          # 预期效应量 >> 噪声底 才为 true
  positive_control: "（阳性对照：人为注入 200ms 延迟，确认该指标按预期抬升）"
  one_variable: "（本实验相对基线唯一改变的量）"
  stop_condition: "（什么情况下提前终止）"
verdict_schema:             # 输出必须是三值之一
  - confirmed               # 证实：效应观测到且超噪声底
  - falsified               # 证伪：效应不存在，且阳性对照通过
  - unresolved              # 无分辨力：判别力不足
rule: |
  resolvable=false 时只允许输出 unresolved。
  排除性结论必须附阳性对照结果。
  实验结束后本文件与结果一同归档，不允许事后修改假设。
```

## 附录 B：passport / 运行指纹模板

```yaml
# 每次运行自动生成，写入输出目录/日志；下游一切分析先验指纹
run_id: 2026-08-27T01:30:00Z
config_hash: sha256:...
config_path: configs/xxx.yaml
code_commit: git:abc1234 (dirty: false)
environment: staging          # 或 prod / local
key_params:                   # 影响结果的关键参数快照
  <domain specific>
passport_check: |
  下游分析脚本/报表开头必须:
  1. 读取本文件, 与预期 config_hash 比对, 不符则拒绝执行并报警
  2. 在产出物中注明 run_id 与 config_hash
  3. dirty=true 的运行结果不得用于任何结论
```

## 附录 C：ADR 模板

```markdown
# ADR-NNNN: 标题
status: Proposed | Accepted | Superseded by ADR-MMMM
supersedes: ADR-KKKK（如有）
date: YYYY-MM-DD
scope: 影响的模块/路径

## 背景与问题
## 选项与权衡（至少两个真实选项，"不做"也是选项）
## 决定
## 依据 artifact（命令+输出/图/数据，必填；纯论证不接受）
## 影响面与回滚方案
## 推翻条件
> 什么证据出现时应重审本决定。写得越具体，将来的推翻越像按预案执行而非打脸。
```

## 附录 D：lesson 卡格式

```markdown
## L-NNNN (YYYY-MM-DD) 一句话教训
- 现象: 踩坑时的可观测表现
- 根因: 确认的根因（附 artifact）
- 误判史: 之前错误排除过什么 ←【假阴性记录，本方法论最看重的字段】
- 机器化: 升级为哪条断言/lint/hook/schema/测试（第二次发生时必填）
- 验证: 该机器检查确实能拦住原始错误的证明（这本身是阳性对照）
- 状态: bank（候选）| promoted（已机器化）| stale（已过期，删除）
```

## 附录 E：Day-0 / Day-1 / Week-1 检查清单

**Day 0（写第一行业务代码之前）**
- [ ] 确定项目级别 T0/T1/T2，并在 AGENTS.md 标出项目内的 T2 区域（§1）
- [ ] 不变量层骨架：核心领域对象的属性测试 + 全局不变量（§3.1）
- [ ] CI 存在且绿：build + lint + format + 密钥扫描 + 单测（§4.7）
- [ ] 配置 schema 校验器（未知键必须报错）+ 运行指纹（§4.6）
- [ ] AGENTS.md（≤100 行，含三档边界 + 安全性能底线 + 证据强度表占位）
- [ ] `docs/adr/`、`docs/lessons.md`、`architecture.md` 占位

**Day 1**
- [ ] `.claude/settings.json` hooks：PostToolUse 快检查 + PreToolUse 危险命令拦截（§4.5）
- [ ] skills：`verify-claims`、`root-cause-debug` 两个先落地（§4.4）
- [ ] 变异测试跑通一次（哪怕只对一个模块），记录基线存活率（§3.3）
- [ ] 证据强度表 v1 写进 AGENTS.md（§3.5）
- [ ] （T2）hypothesis 门禁 + 三道闸写入贡献指南

**Week 1**
- [ ] 第一个垂直切片端到端走完整流程（提案 → 实施 → 不变量全绿 → 归档）
- [ ] 第一次 lessons → 机器检查升级完成（**验证管道会转，比内容更重要**）
- [ ] 复杂性预算阈值 + 架构适应度函数进 CI（§8.5、§8.6）
- [ ] 第一次独立清理 pass

**持续**
- [ ] 每次纠错 → lessons.md；同一坑第二次 → **当天**机器化
- [ ] 每个里程碑 → 独立清理 pass + lessons groom（删除过期教训）
- [ ] 每次推翻 ADR → 附反证 artifact + supersede 链
- [ ] 每月 → 看 §11 五个指标，5 分钟

---

## 最后一句实话

以上没有一条能让 AI 变可靠。**它们做的是把 AI 的错误变得便宜且必然被发现。**

你的目标不该是"找到一个不犯错的 agent"——这个东西不存在。
本方法论的起源事故就是：一个 AI 写下"机制 > 文字"这句话，然后在同一条消息里违反了它；
接着在"核查"另一个 AI 时，把两个真实存在的东西判成了编造的。
**它知道规则，它刚写下规则，它仍然违反规则。**

所以目标是让一个不可信的执行者无法造成持久损害：

> **每个结论带分辨率声明，每个排除带阳性对照，
> 每条规则有执行钩子，每个反驳都要交证据。**

在这个体系里，错误在 30 秒内暴露，而不是靠你恰好点开一个链接。
